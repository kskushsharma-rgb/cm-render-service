{
  "name": "cm-render-service",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Private self-hosted Chromium HTML→PDF service for the CM dashboard report pipeline. Only the Vercel function calls it (shared-secret). Runs on our own Railway infrastructure — report data never touches a third-party PDF vendor.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "puppeteer": "^23.11.1"
  }
}
